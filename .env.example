# Пример файла переменных окружения для проекта Go-LLM-RpgGameMaster
# Скопируйте этот файл в .env и заполните нужные значения.
# Значения ниже — примеры/рекомендованные дефолты для локальной разработки.

# === Telegram Bot ===
# API-токен вашего Telegram-бота (обязательная переменная для запуска бота)
RPG_TELEGRAM_BOT_API_KEY=123456789:AAAbbbCcc_DddEeeFffGggHhh

# === Qdrant (векторное хранилище) ===
# URL инстанса Qdrant. Для локального запуска по умолчанию используется 6333 порт.
QDRANT_URL=http://localhost:6333

# Коллекция Qdrant, используемая приложением. В коде сейчас зашито значение
# "game_collection". Изменение этого значения потребует правок кода.
#QDRANT_COLLECTION=game_collection

# === Ollama (LLM и эмбеддинги) ===
# Имя модели LLM для генерации ответов. В коде по умолчанию используется "llama3.1".
# Можно указать другую модель, если она загружена в Ollama.
OLLAMA_MODEL=llama3.1

# Имя модели для эмбеддингов. По умолчанию используется "nomic-embed-text".
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# Адрес сервера Ollama. По умолчанию локальный сервер на 11434 порту.
# Текущая версия кода использует настройки по умолчанию клиента langchaingo,
# но эта переменная может пригодиться для контейнеризации/деплоя и дальнейшей
# конфигурируемости (потребуются небольшие правки кода для чтения этой переменной).
OLLAMA_SERVER_URL=http://localhost:11434

# === Прочее ===
# Уровень логирования (пример: debug | info | warn | error). Сейчас не используется в коде,
# но может пригодиться при расширении проекта.
#LOG_LEVEL=info
